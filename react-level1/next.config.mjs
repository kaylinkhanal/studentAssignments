/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "example.com",
          "www.theleathershop.com",
          "m.media-amazon.com",
          "images.pexels.com"
        ],
      },
};

export default nextConfig;