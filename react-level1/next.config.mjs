/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "example.com",
            "placeimg.com",
            "www.bbc.co.uk",
            "encrypted-tbn0.gstatic.com",
            "upload.wikimedia.org",
        ]
    }
};

export default nextConfig;
