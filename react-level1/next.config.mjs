/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "source.unsplash.com",
            "fakestoreapi.com",
            "example.com",
            "m.media-amazon.com",
            "images.pexels.com"
        ],
    },
};

export default nextConfig;
